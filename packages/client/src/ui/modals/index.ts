import PasswordModal from "./passModal";
import FileModal from "./fileModal";
import ForumThreadModal from "./forum";

const Modal = {
  Password: PasswordModal,
  File: FileModal,
  ForumThread: ForumThreadModal,
};

export default Modal;
