pre-commit:
  parallel: true
  commands:
    stylelint:
      glob: '*.{css,scss}'
      run: yarn run lint:styles
    prettier:
      glob: '*.{js,jsx,ts,tsx,css,scss}'
      run: yarn run format
      stage_fixed: true
